
# Sudo 

if seeing a permission denied in terminal 

run previous command with sudo

(Prefix) (flag) (Command)

*note first two are optional in general *

# Curl 

curl can be used to download stuff on website 
used for downlaoding fliels teesting apis and checking website health 

example 
curl --proto '=https' --tlsv1.2 -sSf https://rustup.rs | sh
